# TaskFlow Frontend

React + Vite frontend for the TaskFlow team task manager.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Railway

The frontend is built with Vite and served with the bundled Node server in `server.js`.
